{
  "name": "@opentf/workeros-coreutils",
  "version": "0.0.0",
  "description": "WorkerOS coreutils — guest programs (echo, cat, ls, cp, mv, rm, mkdir, pwd, env, true, false, seq, head, tail, wc, sort, uniq, cut, tr) written against the native `sys` syscall ABI.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build:programs": "node tools/bundle.mjs",
    "build": "npm run build:programs",
    "prepack": "npm run build",
    "test": "node --test tools/*.test.js"
  },
  "files": [
    "src"
  ],
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
